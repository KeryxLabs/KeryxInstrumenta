version: '3.8'
services:
  surrealdb:
    image: surrealdb/surrealdb:latest
    container_name: surrealdb
    ports:
      - "9096:8000"
    volumes:
      - ./data:/data
    command:
      - start
      - --user=root
      - --pass=root
      - surrealkv:/data/sttp-mcp.db
    restart: always